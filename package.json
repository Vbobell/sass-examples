{
  "name": "sass-examples",
  "version": "1.0.0",
  "description": "Project to sass + smacss + gulp examples",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "gulp",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "keywords": [
    "sass",
    "smacss",
    "gulp",
    "example"
  ],
  "author": "Vinicius Böbel",
  "license": "ISC",
  "devDependencies": {
    "gulp": "^4.0.0",
    "gulp-concat": "^2.6.1",
    "gulp-sass": "^4.0.2",
    "node-sass": "^4.11.0"
  },
  "dependencies": {
    "gh-pages": "^2.0.1"
  }
}
